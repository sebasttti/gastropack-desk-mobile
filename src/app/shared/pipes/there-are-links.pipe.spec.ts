import { ThereAreLinksPipe } from './there-are-links.pipe';

describe('ThereAreLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new ThereAreLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
