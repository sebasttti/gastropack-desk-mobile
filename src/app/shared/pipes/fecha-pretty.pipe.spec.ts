import { FechaPrettyPipe } from './fecha-pretty.pipe';

describe('FechaPrettyPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaPrettyPipe();
    expect(pipe).toBeTruthy();
  });
});
