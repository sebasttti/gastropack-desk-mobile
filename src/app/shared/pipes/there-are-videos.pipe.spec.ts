import { ThereAreVideosPipe } from './there-are-videos.pipe';

describe('ThereAreVideosPipe', () => {
  it('create an instance', () => {
    const pipe = new ThereAreVideosPipe();
    expect(pipe).toBeTruthy();
  });
});
